# GUI
customtkinter>=5.2.2

# Conexión a Neon PostgreSQL (binary = no requiere compilar nada en la PC del local)
psycopg[binary]>=3.2

# Lectura de credenciales desde .env
python-dotenv>=1.0

# Certificados raíz propios: respaldo para validar HTTPS cuando el almacén de
# Windows de la PC del local no tiene el raíz de GitHub (ver updater.py).
certifi>=2024.2.2
